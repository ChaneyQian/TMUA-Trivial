---
database: TMUA
qid: 20230222000
id: 23-P2-Q20
paper: TMUA P2
year: 2023
number: Q20
section: Reasoning
difficulty: 0
topics: [Logic and Proof, Function, Calculus]
subtopics: [Logic, Absolute Value Functions, Integration]
tags: [Only-If, Contrapositive, Counterexample]
status: 已合审
---

## 题目
Let $f$ be a polynomial with real coefficients.

The integral $I_{p, q}$ where $p < q$ is defined by

$$
I_{p, q} = \int_{p}^{q} \left( (f(x))^2 - (f(|x|))^2 \right) \mathrm{d}x
$$

Which of the following statements must be true?

1. $I_{p, q} = 0$ only if $0 < p$
2. $f'(x) < 0$ for all $x$ only if $I_{p, q} < 0$ for all $p < q < 0$
3. $I_{p, q} > 0$ only if $p < 0$

$$
\mathbf {A} \quad \text{none of them}
$$

$$
\mathbf {B} \quad \text{1 only}
$$

$$
\mathbf {C} \quad \text{2 only}
$$

$$
\mathbf {D} \quad \text{3 only}
$$

$$
\mathbf {E} \quad \text{1 and 2 only}
$$

$$
\mathbf {F} \quad \text{1 and 3 only}
$$

$$
\mathbf {G} \quad \text{2 and 3 only}
$$

$$
\mathbf {H} \quad \text{1, 2 and 3}
$$

## 备注

### 我的备注

### AI备注


## 答案
D

## 解析
We note first that when $x \ge 0 , f ( | x | ) = f ( x )$ , and so for $x \geq 0$ , the integrand is just $\left( f ( x ) \right) ^ { 2 } -$ $\left( f ( x ) \right) ^ { 2 } = 0$ . When $x < 0$. , though, the integrand could take any value.

1 We present a complicated approach first.

There seems to be no obvious reason why this must be true; one could imagine a polynomial for which the values of the integrand are positive for some negative values of $x$ and negative for others, so the positive parts and negative parts cancel out making the integral zero even though $p < 0$ . Here is one example: consider a quartic $f ( x )$ that looks roughly like this:

![[Image/23-P2-Q20-sol1.jpg]]

For negative values of $x$ close to $x = 0 , \left( f ( x ) \right) ^ { 2 } > \left( f ( | x | ) \right) ^ { 2 }$ . But once $x \ < \ x _ { 0 }$ , we have $\left( f ( x ) \right) ^ { 2 } < \left( f ( | x | ) \right) ^ { 2 }$ as $f ( \left| x \right| ) > f ( x )$ . We can therefore take $q = 0$ and find some $p < x _ { 0 }$ for which $I_ {p,q } = 0$:

A much simpler approach is that we can take $p = 0 , q > 0$ and then $I _ { p , q } = 0$ , so this statement is not true. Were the question to have said $0 \leq p$ instead of $0 < p$ , though, we would not have been able to use this simpler approach.

2 We could take $f ( x ) = 1 - x$ , with $f ^ { \prime } ( x ) = - 1 < 0$ for all x.

![[Image/23-P2-Q20-sol2.jpg]]

Then $\left( f ( x ) \right) ^ { 2 } > \left( f ( | x | ) \right) ^ { 2 }$ for all $x < 0$. so $I _ { p , q } > 0$ for all $p < q < 0$

3 We observed at the start of this question that the integrand is zero for all $x \geq 0$ . So if $p \geq 0$ then $I _ { p , q } = \textstyle \int _ { p } ^ { q }$ 0 dx = 0. The contrapositive of this statement is: if $I _ { p , q } \neq 0$. , then $p < 0$ . The given statement $( I _ { p , q } > 0$ only if $p < 0$ , which is the same as saying: if $I _ { p , q } > 0$ , then $p < 0 )$ is a special case of the more general result and is therefore true.

The correct answer is therefore option D, 3 only.
