---
database: TMUA
qid: 20170211900
id: 17-P1-Q19
paper: TMUA P1
year: 2017
number: Q19
section: Applications
difficulty: 0
topics: [Polynomial, Miscellaneous Pure]
subtopics: [Solution of Equations, Inequalities]
tags: [Roots-to-Quadratic-Equations]
status: 已合审
---

## 题目
The set of solutions to the inequality $x^2 + bx + c < 0$ is the interval $p < x < q$ where $b$, $c$, $p$ and $q$ are real constants with $c < 0$.

In terms of $p$, $q$ and $c$, what is the set of solutions to the inequality $x^2 + bcx + c^3 < 0$?

$$
\mathbf{A} \quad \frac{p}{c} < x < \frac{q}{c}
$$

$$
\mathbf{B} \quad \frac{q}{c} < x < \frac{p}{c}
$$

$$
\mathbf{C} \quad pc < x < qc
$$

$$
\mathbf{D} \quad qc < x < pc
$$

$$
\mathbf{E} \quad pc^2 < x < qc^2
$$

$$
\mathbf{F} \quad qc^2 < x < pc^2
$$

## 备注

### 我的备注

### AI备注


## 答案
D

## 解析
We can sketch the graph of $y = x ^ { 2 } + b x + c$ . It intersects the x-axis at $p$ and $q$, and intersects the y-axis at $c < 0$ . So the graph must look like this:

![[Image/17-P1-Q19-sol1.jpg]]

However, the vertex might be either side of the y-axis.

To solve the inequality $x ^ { 2 } + b c x + c ^ { 3 } < 0$. , we need to find where this equals zero. We can use the quadratic formula for this: $x ^ { 2 } + b c x + c ^ { 3 } = 0$ when

$$
{ \begin{array}{r l} & {x = {\frac {- b c \pm {\sqrt {(b c) ^ {2} - 4 c ^ {3}}}}{2}}} \\ & {\quad = {\frac {1}{2}} (- b c \pm {\sqrt {b ^ {2} c ^ {2} - 4 c ^ {3}}})} \\ & {\quad = {\frac {1}{2}} c (- b \pm {\sqrt {b ^ {2} - 4 c}})} \end{array} }
$$

Going back to the original quadratic, we can solve it to find p and q in terms of $b$ and c:

$$
p, q = \frac {1}{2} (- b \pm \sqrt {b ^ {2} - 4 c})
$$

Comparing these, we see that the roots of $x ^ { 2 } +$ bcx $\textit { + c } ^ { 3 }$ are pc and $q c$ in some order. So the solution is C or D.

To distinguish between these possibilities, we need to know which is the larger of $p c$ and $q c$. Note that we were told that $c < 0$ . Since $p < q$ , it follows that $p c > q c$ , and so the answer is D. It turns out that we didn’t actually need our graph sketch.

A somewhat less obvious approach to this question is as follows.

We can divide the second inequality by $c ^ { 2 } > 0$ , so $x ^ { 2 } + b c x + c ^ { 3 } < 0$ is equivalent to

$$
\frac {x ^ {2}}{c ^ {2}} + \frac {b x}{c} + c <   0
$$

or

$$
\left(\frac {x}{c}\right) ^ {2} + b \left(\frac {x}{c}\right) + c <   0
$$

This is the same as our original inequality, just with x replaced by $\textstyle { \frac { x } { c } }$, so its solution is $\begin{array} { r } { p < \frac { x } { c } < q } \end{array}$ Multiplying this by $c < 0$ then gives the solution $p c > x > q c$.
