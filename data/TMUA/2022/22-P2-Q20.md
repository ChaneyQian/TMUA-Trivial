---
database: TMUA
qid: 20220222000
id: 22-P2-Q20
paper: TMUA P2
year: 2022
number: Q20
section: Reasoning
difficulty: 0
topics: [Trigonometry, Function]
subtopics: [Trigonometry, Functions]
tags: [Trigonometric-Functions, Composite-Functions, Range, Iteration]
status: 已合审
---

## 题目
The functions $\mathrm{f}_1$ to $\mathrm{f}_5$ are defined on the real numbers by

$$
\begin{align*}
\mathrm{f}_1(x) &= \cos x \\
\mathrm{f}_2(x) &= \sin(\cos x) \\
\mathrm{f}_3(x) &= \cos(\sin(\cos x)) \\
\mathrm{f}_4(x) &= \sin(\cos(\sin(\cos x))) \\
\mathrm{f}_5(x) &= \cos(\sin(\cos(\sin(\cos x))))
\end{align*}
$$

where all numbers are taken to be in radians.

These functions have maximum values $m_1$, $m_2$, $m_3$, $m_4$ and $m_5$, respectively.

Which one of the following statements is true?

$$
\mathbf{A} \quad m_1, m_2, m_3, m_4 \text{ and } m_5 \text{ are all equal to } 1
$$

$$
\mathbf{B} \quad 0 < m_5 < m_4 < m_3 < m_2 < m_1 = 1
$$

$$
\mathbf{C} \quad m_1 = m_3 = m_5 = 1 \text{ and } 0 < m_2 = m_4 < 1
$$

$$
\mathbf{D} \quad m_1 = m_3 = m_5 = 1 \text{ and } 0 < m_4 < m_2 < 1
$$

$$
\mathbf{E} \quad m_1 = m_3 = 1 \text{ and } 0 < m_2 = m_4 < 1 \text{ and } 0 < m_5 < 1
$$

$$
\mathbf{F} \quad m_1 = m_3 = 1 \text{ and } 0 < m_4 < m_2 < 1 \text{ and } 0 < m_5 < 1
$$

## 备注

### 我的备注

### AI备注


## 答案
E

## 解析
We note that we can rewrite each of the functions $\mathrm { f _ { 2 } }$ to $\mathrm { f _ { 5 } }$ in terms of the previous function:

$$
\begin{array}{l} \mathrm {f_ {1}} (x) = \cos x \\ \mathrm {f_ {2}} (x) = \sin \mathrm {f_ {1}} (x) \\ \mathrm {f_ {3}} (x) = \cos \mathrm {f_ {2}} (x) \\ \mathrm {f_ {4}} (x) = \sin \mathrm {f_ {3}} (x) \\ \mathrm {f_ {5}} (x) = \cos \mathrm {f_ {4}} (x) \end{array}
$$

Since $\operatorname { f } _ { 1 } ( x ) = \cos x$ is periodic with period $2 \pi$ , each of the other functions also repeats every $2 \pi$ (though it may have a shortest period less than this), and so we only need to consider $0 \leq x \leq 2 \pi$ (We could actually restrict ourselves to $0 \leq x \leq \pi$ , as the values of cos x in the range $\pi < x \leq 2 \pi$ are a repeat of the values in the range $0 \leq x \leq \pi$ , but we will draw a complete period anyway.) Sketching the graph of each of these functions is the most straightforward way to proceed. We note first a result that we will need later: $\pi \approx 3 . 1 4$ so $\frac { \pi } { 2 } \approx 1 . 6$ and hence sin $\begin{array} { r } { 1 < \sin \frac { \pi } { 2 } = 1 } \end{array}$ A sketch of f (x) = cos x is straightforward:

![[Image/22-P2-Q20-sol1.jpg]]

We see that $m _ { 1 } = 1$ , and the minimum value taken is −1. Now $\operatorname { f } _ { 2 } ( x ) = \sin \operatorname { f } _ { 1 } ( x )$ , so this goes between − sin 1 and sin 1, with roots where $\mathrm { f } _ { 1 } ( x ) = 0 ;$ ; recall that sin $1 < 1$

![[Image/22-P2-Q20-sol2.jpg]]

We see that $m _ { 2 } = \sin 1 < 1$ , and the minimum value taken is − sin 1.

Now $\operatorname { f } _ { 3 } ( x ) = \cos \operatorname { f } _ { 2 } ( x )$ . When $\mathrm { f _ { 2 } } ( x ) = 0 , \mathrm { f _ { 3 } } ( x ) = 1 , \mathrm { s o f _ { 3 } } ( x )$ has a maximum of 1 at $\begin{array} { r } { x = \frac { \pi } { 2 } } \end{array}$ and at $\begin{array} { r } { x = \frac { 3 \pi } { 2 } } \end{array}$ . But $\mathrm { f _ { 3 } } ( x )$ never reaches 0 as $\operatorname { f } _ { 2 } ( x )$ never reaches $\begin{array} { r } { \frac { \pi } { 2 } \approx 1 . 6 ~ \mathrm{or} ~ - \frac { \pi } { 2 } \approx - 1 . 6 } \end{array}$ . Therefore the graph looks something like this:

![[Image/22-P2-Q20-sol3.jpg]]

Therefore $m _ { 3 } = 1$ , and the minimum value taken is cos(sin 1).

Next, $\operatorname { f } _ { 4 } ( x ) = \sin \operatorname { f } _ { 3 } ( x )$ , so this will be an oscillating curve going between $\sin \bigl( \cos ( \sin 1 ) \bigr)$ and $\sin 1$:

![[Image/22-P2-Q20-sol4.jpg]]

Thus $m _ { 4 } = \sin 1$

Finally, $\mathrm { f } _ { 5 } ( x ) \ = \ \cos \mathrm { f } _ { 4 } ( x )$ , so this will be an oscillating curve going between cos(sin 1) and cos(sin(cos(sin 1))):

![[Image/22-P2-Q20-sol5.jpg]]

To summarise, we have $m _ { 1 } = m _ { 3 } = 1 , m _ { 2 } = m _ { 4 } =$ sin 1 and $m _ { 5 } = \cos ( \sin ( \cos ( \sin 1 ) ) )$ , and the correct option is E.

It is also interesting to note from these accurate graphs that the subsequent functions are getting flatter and flatter and seem to be tending to a straight line (though the limiting value is different in the functions $\mathrm { f } _ { n }$ where n is even and those where n is odd).
